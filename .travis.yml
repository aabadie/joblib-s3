sudo: required

language: python

python:
    - "2.7"
    - "3.4"
    - "3.5"

install:
    - pip install -r requirements.txt -r requirements-tests.txt .

script:
    - pytest

after_success:
    - codecov

notifications:
      email: false
